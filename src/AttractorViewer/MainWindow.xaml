<Window x:Class="AttractorViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AttractorViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="900" KeyDown="Window_KeyDown" ResizeMode="NoResize" Icon="App.ico">
    <Grid Background="#FF1E1E1E" MouseWheel="viewport_MouseWheel" >
        <Viewport3D x:Name="viewport" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,100,0" IsHitTestVisible="False">
            <Viewport3D.Camera>
                <OrthographicCamera x:Name="Camera" Position="-80,-80,100" LookDirection="1,1,-1" UpDirection="0,0,1" Width="100"/>
            </Viewport3D.Camera>
        </Viewport3D>
        <Button Content="Open" Margin="0,30,10,0" VerticalAlignment="Top" Click="Button_Click" HorizontalAlignment="Right" Width="75"/>
        <CheckBox x:Name="cboxTrajectory" IsChecked="False" Content="Trajectory" HorizontalAlignment="Left" Margin="809,10,0,0" VerticalAlignment="Top" Foreground="#FFC5C5C5" RenderTransformOrigin="0.507,-0.8"/>
    </Grid>
</Window>
